# Phase 5 — Password reset

## Delivered
- Policy + strength meter on `/reset-password`
- Expiry / single-use friendly messaging; global sign-out after change
- Existing forgot-password rate limit + branded reset template helper

## Notes
Token expiry/single-use enforced by Supabase recovery links.