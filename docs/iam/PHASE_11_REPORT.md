# Phase 11 — Security hardening

## Delivered
- CSP + COOP headers in `next.config.ts`
- CSRF same-origin check on admin login
- Cookie SameSite=lax already on platform sessions
- Customer login rate limit + lockout
- Error sanitisation hardened