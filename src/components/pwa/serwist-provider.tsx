/**
 * @deprecated Use `PwaServiceWorkerRegistrar` via `DeferredPwaShell` instead.
 * Kept for backwards compatibility with docs and imports.
 */
export { PwaServiceWorkerRegistrar as PwaSerwistProvider } from "./pwa-service-worker-registrar";
